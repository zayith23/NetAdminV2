
  # NetAdmin
  

  ## Running the code

  Run `npm i` para instalar dependencias.

  Run `npm run dev` para iniciar el servidor de desarrollo.

  ## Produccion del codigo 

   ` Cd build `
  ` npm run build`
  Hit CTRL-C to stop the server  
