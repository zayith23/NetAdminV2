
  # NetAdmin

  This is a code bundle for NetAdmin. The original project is available at https://www.figma.com/design/FWjOxuqYQTDdPOs9RcHb5k/NetAdmin.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  